// Vendor Imports
import styled from 'styled-components';

/**
 * ContentSectionWrapper
 * Gives the content section some padding as well as a center alignment on larger screens
 */
const ContentSectionWrapper = styled.div`
  min-height: 100%;
  width: 100%;
  max-width: 90rem;
  padding: 1rem;
`;

export default ContentSectionWrapper;
