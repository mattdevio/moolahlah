// Vendor Imports
import styled from 'styled-components';

/**
 * ContentSectionContainer
 * The container the wraps the main content section
 */
const ContentSectionContainer = styled.div`
  width: calc(100% - 40rem);
  height: 100%;
  min-height: 100%;
  padding: 0;
`;

export default ContentSectionContainer;
