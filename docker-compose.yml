version: "2"

services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www
    ports:
      - "3000:3000"
    networks:
      - moolahlah-network

  mongodb:
    image: mongo
    networks:
      - moolahlah-network

networks:
  moolahlah-network:
    driver: bridge